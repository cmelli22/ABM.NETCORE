import NavBar from './NavBar'

const Header = ({ className }) => {

    return (
        <header className={className}>
            <NavBar></NavBar>
        </header>
    )

} 

export default Header;